# MoveQuest runs fully self-contained — these are only needed if you wire up the
# production Supabase + AI backend described in README.md / supabase/schema.sql.

# Supabase (optional)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# AI quest generation (optional — falls back to the built-in engine)
# Provide one of these and add an API route that returns the Quest[] shape.
GEMINI_API_KEY=
OPENAI_API_KEY=
